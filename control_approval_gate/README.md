# mn-agents.control_approval_gate

Gates workflow progress on human or policy approval state.

Category: `control`.

Kind: `router`.

Required inputs: `approval_mode`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
