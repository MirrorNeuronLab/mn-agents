# mn-agents.data_openshell_service

Custom OpenShell service with explicit port and SSH tunnel verification.

Version: `1.0.0`.

Required inputs: `custom_openshell_image, container_port, external_port, tunnel`.
