# mn-agents.data_sandboxed_codegen

Runs code-generation or code-review work inside an explicit sandbox boundary.

Category: `data`.

Kind: `executor`.

Required inputs: `llm_config`, `task`, `sandbox`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
