# mn-agents.control_lifecycle

Emits workflow lifecycle control messages such as start, stop, pause, and resume.

Category: `control`.

Kind: `router`.

Required inputs: `action`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
