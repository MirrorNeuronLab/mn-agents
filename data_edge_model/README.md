# mn-agents.data_edge_model

Runs a local or edge model with declared device and resource profile.

Category: `data`.

Kind: `executor`.

Required inputs: `model_config`, `device_profile`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
