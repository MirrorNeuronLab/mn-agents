# mn-agents.data_observer

Observes a stream, file, sensor, or service and emits normalized observations.

Category: `data`.

Kind: `executor`.

Required inputs: `observation_source`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
