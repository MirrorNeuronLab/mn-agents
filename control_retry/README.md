# mn-agents.control_retry

Routes retry decisions with bounded attempts and backoff metadata.

Category: `control`.

Kind: `router`.

Required inputs: `retry_policy`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
