# mn-agents.data_llm_tool

Runs an LLM-backed tool-using agent through named LLM config and declared tools.

Category: `data`.

Kind: `executor`.

Required inputs: `llm_config`, `tools`, `role`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
