# mn-agents.control_message_filter

Filters or gates messages using a declarative predicate name.

Category: `control`.

Kind: `router`.

Required inputs: `predicate`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
