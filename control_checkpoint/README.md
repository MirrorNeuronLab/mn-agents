# mn-agents.control_checkpoint

Records checkpoint and resume metadata for stateful workflows.

Category: `control`.

Kind: `executor`.

Required inputs: `checkpoint_key`.

This is a generic agent template. Blueprints actualize it with `uses`, `with`, and optional `config` overrides.
