{
  "defaults": {
    "agent_type": "module",
    "module": "MirrorNeuron.Agents.StreamTickSource",
    "type": "stream"
  },
  "description": "Reusable stream or polling tick source for live and replayed stream workflows.",
  "input_spec": "input.spec.json",
  "inputs": {
    "optional": [
      "node_type",
      "role",
      "interval_ms",
      "backpressure",
      "stream_name"
    ],
    "required": [
      "emit_type"
    ]
  },
  "kind": "listener",
  "output_spec": "output.spec.json",
  "payloads": "payloads",
  "provides": [
    "stream_ticks",
    "backpressure"
  ],
  "renders_to": "manifest.nodes[]",
  "template_id": "mn-agents.stream_tick_source",
  "version": "1.0.0"
}
